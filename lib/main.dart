import 'package:flutter/material.dart';
import 'package:value_notifier/src/app_widget.dart';

void main() {
  runApp(const AppWidget());
}

// Usando dartion como backend, para ativar digite no terminal
// flutter pub global activate dartion e se precisar configura as variaveis de ambiente
// No terminal digita dartion init, e para rodar dartion serve